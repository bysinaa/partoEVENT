# Parto CMS Handoff

## Final status

`SESSIONS 1-6: PASS` (final verification 2026-08-11)

The production-hardening sequence is complete. Current code and
`docs/CMS-CONTRACTS.md` are canonical.

## Final architecture

- Website: Next.js, `src/`, port `3000`.
- Admin: Next.js, `parto-cms/apps/admin`, port `3003`.
- API: NestJS, `parto-cms/apps/api`, port `3006`, prefix `/api/v1`.
- Database: PostgreSQL + Prisma.
- CMS: custom self-hosted Parto CMS. Sanity is removed and must not be
  reintroduced.

## Session status

- Session 1 — Project contract: `PASS`. Canonical DTO/service/Prisma/public/
  website names and published filtering are aligned.
- Session 2 — Client, Service, Team: `PASS`. Localization, media IDs, and
  relation edit hydration round-trip without loss.
- Session 3 — Media pipeline: `PASS`. Upload validation, embedded public media,
  website rendering, batched resolution, and physical deletion are covered.
- Session 4 — Project/Client relations: `PASS`. `ProjectClient` and `clientIds`
  are the only relation source of truth and synchronize atomically.
- Session 5 — Settings/freshness: `PASS`. Settings persist through the Admin and
  public allow-list; all website CMS reads use `no-store`.
- Session 6 — Production hardening/final E2E: `PASS`. Known seed passwords are
  gone, production JWT and seed configuration fails closed, obsolete artifacts
  are removed, README is current, and the final HTTP smoke flow passes.

## Session 6 changes

Root causes:

- Seed execution fell back to predictable Admin/Editor passwords and printed
  configured secrets.
- Production API startup did not validate missing or example JWT secrets.
- The pre-migration architecture report and a hardcoded legacy API script were
  obsolete; a tracked Prisma temporary engine file was generated runtime debris.
- README still documented predictable development credentials and claimed no
  automated tests.

Canonical decisions:

- Production seed passwords must be supplied by environment. Development may
  omit them, in which case cryptographically random one-time passwords are
  generated and printed.
- Production requires non-placeholder access and refresh JWT secrets of at
  least 32 characters.
- Final E2E uses the existing Nest app, native `fetch`, and the real local
  PostgreSQL schema; no E2E framework dependency was added.
- Production deployment uses three multi-stage Node 22 images, PostgreSQL 16,
  and Caddy as the only public entry point with automatic HTTPS.
- A tracked initial Prisma migration supports fresh VPS databases; API startup
  applies pending migrations before starting Nest.
- The smoke creates a unique authenticated user, Media, Client, Project, and
  Settings change; it updates/publishes content, verifies public responses and
  website mappers, then cleans up.
- Generated Prisma Client remains generated by `prisma generate` and is never
  manually edited. Temporary engine copies are ignored.
- Portable Text packages/components remain because current website code still
  imports them; they are not unused Sanity configuration.

## Verification results

- `npm test` in API: `PASS` — 23/23 focused contract/security tests.
- `npm run test:smoke` in API: `PASS` — 1/1 live HTTP/PostgreSQL flow.
- `npm run build` in API: `PASS`.
- `npx tsc -p prisma/tsconfig.json --noEmit`: `PASS`.
- `npm run type-check` and `npm run build` in Admin: `PASS`.
- `npm test` in Website: `PASS` — 47/47.
- `npm run typecheck` and `npm run build` in Website: `PASS`.
- `npx prisma validate`: `PASS`.
- `npx prisma generate`: `PASS` (Prisma Client 6.19.3).
- `git diff --check`: `PASS`.
- Website/Admin/API Docker image builds: `PASS`.
- Fresh isolated production stack: `PASS` â€” initial migration applied,
  PostgreSQL/API healthy, Website and Admin returned HTML, public API and seeded
  Admin login returned HTTP 200.
- Production dependency audits (Website and CMS): `PASS` â€” 0 vulnerabilities.
- `docker compose ... config --quiet`: `PASS`.

## Intentional limitations / non-critical work

- Fresh databases are supported by the tracked initial migration. A database
  previously created with `prisma db push` must be backed up and baselined with
  `prisma migrate resolve` before connecting this deployment.
- Media storage is the API's local `uploads` directory. Production must provide
  the named `api_uploads` volume (included) or migrate it to object storage.
- The public read API is intentionally unauthenticated and has no application
  rate limiter; deploy it behind normal edge rate limiting/protection.
- CMS reads intentionally use `no-store` for reliable freshness. Add targeted
  invalidation only if measured traffic justifies caching complexity.

## Next session

None. Session 6 is `PASS`; stop.
